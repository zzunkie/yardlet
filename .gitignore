# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Yard runtime state created in workspaces under test
# (the canonical .agents/ of a *consuming* repo is theirs to track;
#  inside this repo we only keep templates/ and seeds/)
/.agents/runs/
/.agents/checkpoints/
/.agents/handoffs/

# OS / editor
.DS_Store
*.swp
