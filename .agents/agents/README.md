# Shared Agents

No shared specialist agent prompts are defined for this repository yet.

If reusable reviewer roles are added later, store the prompt source in `.agents/agents/` and keep tool-specific wrappers (`.claude/`, `.codex/`) as symlinks into here.
